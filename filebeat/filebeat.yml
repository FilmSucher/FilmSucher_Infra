filebeat.inputs:
- type: log
  paths:
    - /var/log/myapp/*.log
  json.keys_under_root: true
  json.add_error_key: true
  fields:
    type: frontend   # уникальный type для машины 101

output.logstash:
  hosts: ["192.168.56.104:5044"]